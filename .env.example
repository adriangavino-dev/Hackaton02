# --- Desarrollo local ---
# Usa el proxy de Vite (mismo origen) para evitar CORS y permitir PATCH.
VITE_API_BASE_URL=/api/v1
# Destino del proxy de /api en desarrollo: el backend desplegado del profesor.
VITE_PROXY_TARGET=https://hackaton-20261-front-587720740455.us-east1.run.app

# --- Produccion (Vercel/Netlify) ---
# En el deploy NO se usa proxy: define la URL COMPLETA del backend, con /api/v1.
# VITE_API_BASE_URL=https://hackaton-20261-front-587720740455.us-east1.run.app/api/v1

# Autocompletado del login en desarrollo (ajusta a las credenciales de TU equipo).
VITE_TEAM_CODE=TEAM-001
VITE_EMAIL=operator@tuckersoft.com
VITE_PASSWORD=Pizza-TEAM-001
