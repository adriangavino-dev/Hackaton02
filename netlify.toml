[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: cualquier ruta sirve index.html para que el deploy abra
# directamente en /dashboard, /signals/:id, /sectors/:id/story, etc.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
